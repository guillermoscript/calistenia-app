---
category: Feedback
---

La imagen. Necesita `alt`.

Parte de la familia `Avatar`. Se usa dentro de `<Avatar>`, no por separado.

## Composición

```jsx
<Avatar>
  <AvatarImage src={usuario.avatar} alt={usuario.nombre} />
  <AvatarFallback>GM</AvatarFallback>
</Avatar>
```

