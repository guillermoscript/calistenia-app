---
category: Forms
---

Etiqueta de campo. Necesita `htmlFor` apuntando al `id` del control — sin eso no hay accesibilidad ni área de clic ampliada.

## Composición

```jsx
<Label htmlFor="peso">Peso corporal (kg)</Label>
<Input id="peso" type="number" inputMode="decimal" />
```

