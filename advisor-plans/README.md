# Advisor Plans — mobile UI parity

Generated by the `improve` skill on 2026-06-15.

> Why `advisor-plans/` and not `plans/`? The existing `plans/` directory is
> dedicated to an unrelated effort (the TanStack Query migration review,
> plans 001–012). Per the skill's convention, unrelated planning work goes here
> to avoid colliding with that numbering and purpose.

**Target branch:** `feat/mobile-data-perf` @ commit `186f071` (current branch).
The file these plans describe is committed there. Do NOT push, merge, rebase, or
open a PR without the maintainer's say-so. Use explicit file paths in `git add`.

This is a single focused `plan <description>` request (not an audit): port the
web app's circular timer UI to the native timed-exercise screen. No runtime
testing was done — the maintainer verifies on device.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Mobile timed-exercise timer: blue circular ring + 3s pre-countdown (web parity) | P2 | M | LOW | — | DONE (executor commit `e90c1d2`, worktree `worktree-agent-a2366ae1bfbe8670b`; awaiting maintainer apply onto `feat/mobile-data-perf`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Notes

- Plan 001 only touches `apps/mobile/src/components/SessionView.tsx`. It rewrites
  the `ExerciseTimer` component to mirror `apps/web/src/components/Timer.tsx`,
  reusing the SVG-ring pattern already proven by `RestScreen` in the same file
  and the existing sounds/haptics/i18n keys (`timer.*`).
- Deferred out of scope: the animated number-pulse and ring-glow motion that web
  does via CSS keyframes (would require `reanimated`); left out to keep risk LOW.
